% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vztdraw_intensity_step.R
\name{vztdraw_intensity_step}
\alias{vztdraw_intensity_step}
\title{Vectorized sampling from a K-truncated NHPPP given the intensity function,
with piecewise constant majorizers over arbitrary interval bounds (R)}
\usage{
vztdraw_intensity_step(
  lambda = NULL,
  lambda_args = NULL,
  Lambda_maj_matrix = NULL,
  lambda_maj_matrix = NULL,
  time_breaks = NULL,
  t_min = NULL,
  t_max = NULL,
  tol = 10^-6,
  atmost1 = FALSE,
  report_first_K = NULL,
  report_last_K = NULL,
  generate_at_least_K = 1,
  generate_at_most_K = NULL,
  budget_cap = NULL
)
}
\arguments{
\item{lambda}{(function) intensity function, vectorized}

\item{lambda_args}{(list) optional named list of arguments to pass to \code{lambda}.
If you have arguments for \code{lambda} that vary by draw, they should be passed as
a data.table named \code{vector_arguments}.}

\item{Lambda_maj_matrix}{(matrix) integrated majorizer intensity rates at
the end of each interval}

\item{lambda_maj_matrix}{(matrix) majorizer intensity rates, one per interval}

\item{time_breaks}{(vector | matrix) the bounds of the majorizer intervals.
With \code{K} intervals (\verb{K = ncol([Lambda|lambda]_maj_matrix)}), either a
vector of \code{K+1} increasing values (the same bounds for all point
processes) or a matrix with \code{K+1} columns whose i-th row holds the
bounds for the i-th point process (a 1-row matrix is recycled for
all point processes).}

\item{t_min}{(scalar | vector | column matrix) is the lower bound
of a subinterval of \verb{(time_breaks[, 1], time_breaks[, K+1]]}. If set,
times are sampled from the subinterval.}

\item{t_max}{(scalar | vector | column matrix) the upper bound
of a subinterval of \verb{(time_breaks[, 1], time_breaks[, K+1]]}. If set,
times are sampled from the subinterval.}

\item{tol}{(scalar, double) tolerance for the number of events}

\item{atmost1}{boolean, report at most 1 event time (alias for
\code{report_first_K = 1})}

\item{report_first_K}{\code{NULL} or a positive integer: report only the
earliest K accepted event times (reporting truncation — the count
law is unchanged). At most one of \code{report_first_K}/\code{report_last_K}
may be set.}

\item{report_last_K}{\code{NULL} or a positive integer: report only the latest
K accepted event times (ascending order; reporting truncation).}

\item{generate_at_least_K}{non-negative integer: condition on at least K
accepted events. The default 1 is the zero-truncated process.}

\item{generate_at_most_K}{\code{NULL} or a positive integer: condition on at
most K accepted events (rejection resampling on the upper bound).}

\item{budget_cap}{\code{NULL} or a positive integer: cap the computational event
budget of the majorizer kernel (approximation knob).}
}
\description{
Thinning sampler conditional on the accepted event count lying in
\verb{[generate_at_least_K, generate_at_most_K]} (\code{generate_at_least_K = 1}
alone is the zero-truncated process), with majorizer intervals of
arbitrary length (\code{time_breaks}, as in \code{vdraw_sc_step()}).

Conditioning is by rejection: candidate realizations are proposed from the
majorizer conditioned on at least K1 majorizer events (only the lower
bound may be pushed into the proposal — at least K1 accepted events
implies at least K1 majorizer events, whereas an upper bound on the
majorizer count would over-restrict and bias the accepted law), thinned
against \code{lambda}, and rows whose surviving count falls outside \verb{[K1, K2]}
are resampled until the condition holds. The per-round acceptance
probability degrades in the strictness of the bounds and in the looseness
of the majorizer, so a tight majorizer matters much more here than in the
unconditional sampler. There is no iteration cap.
}
\keyword{internal}
