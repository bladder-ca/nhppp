% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_sc_step.R
\name{draw_sc_step}
\alias{draw_sc_step}
\title{Simulate a piecewise constant-rate Poisson Point Process over \verb{(t_min, t_max]} (inversion method)
The intervals need not have the same length.}
\usage{
draw_sc_step(
  lambda_vector,
  time_breaks,
  atmost1 = FALSE,
  atmostK = NULL,
  atleast1 = FALSE,
  atleastK = NULL
)
}
\arguments{
\item{lambda_vector}{(scalar, double) \code{K} constant rates, one per interval}

\item{time_breaks}{(vector, double) \code{K+1} time points defining \code{K} intervals
of constant rates:
\verb{[t_1 = range_t[1],       t_2)}: the first interval
\verb{[t_k,                t_\{k+1\})}: the \code{k}-th interval
\verb{[t_\{K\}, t_\{K+1\} = range_t[2])}: the \code{K}-th (last) interval}

\item{atmost1}{boolean, report at most 1 event time (alias for \code{atmostK = 1})}

\item{atmostK}{\code{NULL} or a positive integer: report only the earliest K
event times. Generalizes \code{atmost1}.}

\item{atleast1}{boolean, condition on at least 1 event (alias for \code{atleastK = 1})}

\item{atleastK}{\code{NULL} or a positive integer: condition on at least K events
in the interval. Generalizes \code{atleast1}.}
}
\value{
a vector of event times t
if no events realize, it will have 0 length
}
\description{
Simulate a piecewise constant-rate Poisson Point Process over \verb{(t_min, t_max]} (inversion method)
The intervals need not have the same length.
}
\examples{
x <- draw_sc_step(lambda_vector = rep(1, 5), time_breaks = c(0:5))
}
