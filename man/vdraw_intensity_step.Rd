% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vdraw_intensity_step.R
\name{vdraw_intensity_step}
\alias{vdraw_intensity_step}
\title{Vectorized sampling from NHPPPs given the intensity function, with
piecewise constant majorizers over arbitrary interval bounds (C++)}
\usage{
vdraw_intensity_step(
  lambda = NULL,
  lambda_args = NULL,
  Lambda_maj_matrix = NULL,
  lambda_maj_matrix = NULL,
  time_breaks = NULL,
  t_min = NULL,
  t_max = NULL,
  tol = 10^-6,
  atmost1 = FALSE,
  report_first_K = NULL,
  report_last_K = NULL,
  atleast1 = FALSE,
  generate_at_least_K = NULL,
  generate_at_most_K = NULL,
  budget_cap = NULL
)
}
\arguments{
\item{lambda}{(function) intensity function, vectorized}

\item{lambda_args}{(list) arguments for \code{lambda}, with up to two elements:
\code{shared} (named list of row-invariant arguments of any type, stored
once and never replicated) and \code{row_args} (data.frame or data.table
with one row per point process, auto-subset when rows are resampled).
When non-\code{NULL}, the container is passed as \code{lambda}'s second
positional argument exactly as given, except that \code{row_args} is
already row-subset; the name of the second formal is up to you. A
flat list with neither element is treated as all-shared; a
\code{vector_arguments} element is deprecated (use \code{row_args}).}

\item{Lambda_maj_matrix}{(matrix) integrated majorizer intensity rates at
the end of each interval}

\item{lambda_maj_matrix}{(matrix) majorizer intensity rates, one per interval}

\item{time_breaks}{(vector | matrix) the bounds of the majorizer intervals.
With \code{K} intervals (\verb{K = ncol([Lambda|lambda]_maj_matrix)}), either a
vector of \code{K+1} increasing values (the same bounds for all point
processes) or a matrix with \code{K+1} columns whose i-th row holds the
bounds for the i-th point process (a 1-row matrix is recycled for
all point processes).}

\item{t_min}{(scalar | vector | column matrix) is the lower bound
of a subinterval of \verb{(time_breaks[, 1], time_breaks[, K+1]]}. If set,
times are sampled from the subinterval.}

\item{t_max}{(scalar | vector | column matrix) the upper bound
of a subinterval of \verb{(time_breaks[, 1], time_breaks[, K+1]]}. If set,
times are sampled from the subinterval.}

\item{tol}{(scalar, double) tolerance for the number of events}

\item{atmost1}{boolean, report at most 1 event time (alias for
\code{report_first_K = 1})}

\item{report_first_K}{\code{NULL} or a positive integer: report only the
earliest K accepted event times (reporting truncation — the count
law is unchanged). At most one of \code{report_first_K}/\code{report_last_K}
may be set.}

\item{report_last_K}{\code{NULL} or a positive integer: report only the latest
K accepted event times (ascending order; reporting truncation).}

\item{atleast1}{boolean, condition on at least 1 accepted event (alias for
\code{generate_at_least_K = 1})}

\item{generate_at_least_K}{\code{NULL} or a positive integer: condition on at
least K accepted events (rejection resampling; see
\code{vztdraw_intensity_step()}).}

\item{generate_at_most_K}{\code{NULL} or a positive integer: condition on at
most K accepted events (rejection resampling on the upper bound).}

\item{budget_cap}{\code{NULL} or a positive integer: cap the computational event
budget of the majorizer kernel (approximation knob).}
}
\value{
a matrix of event times (columns) per draw (rows);
NAs are structural empty spots
}
\description{
Vectorized thinning sampler: candidate times are proposed from a piecewise
constant majorizer and accepted with probability
\verb{lambda(t) / lambda_maj(interval of t)}. Unlike the \code{sc_step} samplers,
\code{lambda} is an arbitrary (vectorized) R function — the step structure only
concerns the majorizer. The majorizer intervals need not have the same
length: their bounds are given in \code{time_breaks}, either once for all point
processes or per point process. This generalizes
\code{vdraw_intensity()}, which assumes equal-length ("regular") intervals.
}
\examples{
x <- vdraw_intensity_step(
  lambda = function(x, ...) 0.1 * x,
  lambda_maj_matrix = matrix(rep(1, 5), nrow = 1),
  time_breaks = c(1, 1.5, 3, 4, 4.5, 5)
)
}
