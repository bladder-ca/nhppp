% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vztdraw_sc_step.R
\name{vztdraw_sc_step}
\alias{vztdraw_sc_step}
\title{Vectorized sampling from zero-truncated NHPPPs with piecewise constant
intensities over arbitrary interval bounds (C++)}
\usage{
vztdraw_sc_step(
  lambda_matrix = NULL,
  Lambda_matrix = NULL,
  time_breaks = NULL,
  t_min = NULL,
  t_max = NULL,
  atmost1 = FALSE
)
}
\arguments{
\item{lambda_matrix}{(matrix) intensity rates, one per interval}

\item{Lambda_matrix}{(matrix) integrated intensity rates at the end of each interval}

\item{time_breaks}{(vector | matrix) the bounds of the intervals over which
the rates apply. With \code{K} intervals (\verb{K = ncol([Lambda|lambda]_matrix)}),
either a vector of \code{K+1} increasing values (the same bounds for all
point processes) or a matrix with \code{K+1} columns whose i-th row holds
the bounds for the i-th point process (a 1-row matrix is recycled for
all point processes).}

\item{t_min}{(scalar | vector | column matrix) is the lower bound
of a subinterval of \verb{(time_breaks[, 1], time_breaks[, K+1]]}. If set,
times are sampled from the subinterval.
If omitted, it is equivalent to \code{time_breaks[, 1]}.}

\item{t_max}{(scalar | vector | column matrix) the upper bound
of a subinterval of \verb{(time_breaks[, 1], time_breaks[, K+1]]}. If set,
times are sampled from the subinterval.
If omitted, it is equivalent to \code{time_breaks[, K+1]}.}

\item{atmost1}{boolean, draw at most 1 event time}
}
\value{
a matrix of event times t, with rows corresponding to the sampled point processes.
}
\description{
Simulate piecewise constant-rate Poisson Point Processes conditional on
observing at least one event, where the intervals need not have the same
length. The interval bounds are given in \code{time_breaks}, either once for all
point processes or per point process. This generalizes the zero-truncated
regular-grid sampler used by \code{vdraw_sc_step_regular(atleast1 = TRUE)}.
}
\examples{
x <- vztdraw_sc_step(
  lambda_matrix = matrix(rep(1, 50), nrow = 10),
  time_breaks = c(100, 100.5, 102, 106, 109, 110),
  atmost1 = FALSE
)
}
