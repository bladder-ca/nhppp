# Return matrix with column-wise cumulative sum No checks for arguments is done.

Return matrix with column-wise cumulative sum No checks for arguments is
done.

## Usage

``` r
mat_cumsum_columns(X)
```

## Arguments

- X:

  (matrix)

## Value

matrix
