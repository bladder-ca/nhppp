# Return matrix with column-wise differencing. No checks for arguments is done.

Return matrix with column-wise differencing. No checks for arguments is
done.

## Usage

``` r
mat_diff_columns(X)
```

## Arguments

- X:

  (matrix)

## Value

matrix
